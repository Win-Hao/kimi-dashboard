## What / 改了什么

## Why / 为什么

## Checklist
- [ ] Test first: new behaviour has a failing test that now passes (`npm test`)
- [ ] `npm run lint && npm run build && npm run bench` pass
- [ ] `dist/cli.js` rebuilt and committed (CI checks it is fresh)
- [ ] README (中文 + English) / `commands/*.md` updated if user-visible
- [ ] `kimi.plugin.json` version still equals `package.json` version
